# Templates

This directory contains reusable workflow templates for session state and policy
documents.

Use these templates when bootstrapping a new kit instance or when resetting a
slice to a known-good shape.

Template conventions:

- Keep placeholders explicit and minimal.
- Keep YAML templates aligned with authoritative state keys.
- Keep prose templates short and directive.

