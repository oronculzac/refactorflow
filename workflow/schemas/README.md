# Schemas

This directory is reserved for schema notes and future machine-readable schema
artifacts.

Current conventions:

- YAML files are authoritative for workflow state and policy.
- NDJSON is used for append-only decision history.
- Template files remain human-editable bootstrap text.

If a schema is added later, document its purpose, versioning, and validation
expectations here before relying on it in automation.

